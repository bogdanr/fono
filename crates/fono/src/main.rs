// SPDX-License-Identifier: GPL-3.0-only
//! `fono` — entry point for the voice-dictation daemon and CLI.
//!
//! This crate is a stub; the Phase 8 work in
//! `docs/plans/2026-04-24-fono-design-v1.md` will flesh it out with the
//! first-run wizard, `clap`-powered subcommands, and daemon orchestration.

fn main() {
    println!(
        "fono v{} — scaffold; run 'cargo run -- --help' once Phase 1 lands",
        env!("CARGO_PKG_VERSION")
    );
}
